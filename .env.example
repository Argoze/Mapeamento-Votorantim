# Copie este arquivo para ".env" e preencha com as chaves PÚBLICAS do seu projeto Supabase
# (Project Settings > API). Nunca coloque a Service Role Key aqui — veja o README,
# seção "Criação de usuários (Edge Function)".

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
